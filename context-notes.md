# 컨텍스트 노트 — 지표 기반 개선 (2026-08-03)

## 왜 이 작업을 했나
2026-07-03~08-03 토스 지표에서 D1 리텐션 1.4%, AU 중 신규 88%, 평균 체류 18.9초가 나왔다.
"AU = 토스 검색 노출"인 상태이고 앱 자체는 유저를 붙잡지 못하고 있었다.
코드를 읽어보니 지표의 각 항목에 대응하는 원인이 있어서 그 순서대로 고쳤다.

## 결정과 근거

### 부팅을 2단계로 쪼갬 (startApp → loadRemoteData)
기존에는 deals.json(1.9MB) + 시/도 매장 파일(경기도 2.5MB) + VWorld 역지오코딩(최대 5초)이
전부 직렬로 끝나야 initMap()이 불렸다. 지도 한 픽셀 나오기 전에 4MB+ 대기.
지도를 먼저 그리고 데이터는 도착하는 대로 얹는 구조로 바꿨다.

지도 렌더와 데이터 로딩이 병렬이 되면서 "위치 확정"과 "데이터 도착"의 순서가 비결정적이 됐다.
`dataReady` / `userCoords` 두 플래그로 상태를 들고, 매장 반영은 `applyStoresForLocation()`
한 곳으로 모아서 어느 쪽이 먼저 끝나든 같은 경로를 타게 했다.

### cache:'no-store' 제거
deals.json 요청에 붙어 있어서 매 방문마다 1.9MB를 새로 받았고, sw.js의 network-first
캐시 폴백도 무력화됐다. URL에 이미 `?v=YYYYMMDD`가 붙어 있어 날짜 단위 무효화는 유지된다.

### 데모 DB 제거
MASTER_DB 기본값이 데모 상품 6개였고, deals.json 실패 시 console.log만 남기고 그대로 앱이 떴다.
사용자에겐 "행사가 6개뿐인 앱"으로 보였다. 빈 배열로 시작하고 실패는 `showDataError()`로 명시.

### dealsByBrand 캐시
매장마다 MASTER_DB 전체를 filter했는데 조건이 `p.events[store.brand]` 하나뿐이라
결과는 브랜드 4종밖에 없었다. 브랜드별 1회 계산 후 같은 배열을 참조.
실측 8,578개 매장 × 4,550개 상품 = 3,900만 회 연산이 4회로 줄었다.
MASTER_DB 교체 시 `dealsByBrandCache = null`로 무효화 필요 (재시도 경로 주의).

### 알림 경로 (가장 중요한 미완 항목)
기존 리텐션 알림은 sw.js 안의 setTimeout에 의존했다. 서비스워커는 유휴 시 종료되므로
타이머가 증발하고, 토스 인앱 WebView에는 서비스워커/Notification API 자체가 없다.
유입 데이터에서 푸시가 7/17~7/29에만 잡히고 사라진 것과 정합한다.

토스 SDK를 확인한 결과 `requestNotificationAgreement({options:{templateCode}, ...})`가 있다.
동의만 받는 API이고 **실제 발송은 토스 콘솔 템플릿으로 서버가 보낸다.**
그래서 클라이언트는 동의 획득까지만 하고, 발송은 별도 구성이 필요하다.

**미완**: `ads.js`의 `NOTIFICATION_TEMPLATE_CODE`가 빈 문자열이다.
토스 콘솔에서 템플릿을 발급받아 채워야 동작한다. 비어 있으면
`tossNotificationSupported()`가 false를 반환해 토스 안에서 알림 토글이
"지원하지 않습니다" 토스트를 낸다 — 잘못 켜졌다고 착각하는 것보다 낫다고 판단.

### 폰트 상향
40대 이상이 46.4%인데 본문이 8~11px였고, viewport에 `user-scalable=no`가 있어
확대도 막혀 있었다. `user-scalable=no`/`maximum-scale` 제거 후 8px→10px, 9px→11px.
10/11px은 건드리지 않았다 — 레이아웃 파손 위험 대비 이득이 작다.

### API 키
VWORLD_KEY/KMA_KEY는 정적 배포라 소스에서 숨길 방법이 없다.
코드 주석으로 도메인 제한 필요성만 명시했다. **콘솔 설정은 사람이 해야 하는 미완 항목.**

## 남은 것
- 토스 알림 템플릿 코드 입력 + 서버 발송 구성 (리텐션 개선의 실제 레버)
- VWorld 콘솔 도메인 제한
- 로딩 완료 시각 / 첫 상호작용 이벤트 계측 — 지금은 "로딩 중 이탈"과
  "원하는 상품을 못 찾아 이탈"을 구분할 데이터가 없다
